module QuizzesHelper

  def process_input_file(file, quiz)
    # file = Xsv.open(file)
    # sheet = file.sheets[0]

    # sheets.each do |row|
    #   q = Question.new()
    #   q.question = row[0]
    #   q.answer = row[-1]
    #   q.possible_answers = [1..-2]
    #   q.quiz_id = quiz.id
    #   q.tag = quiz.tag
    #   unless q.save!
    #     break
    #   end

    # end

  end
end
